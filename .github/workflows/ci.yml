name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  AWS_PRODUCTION_ROLE_ARN: arn:aws:iam::049991620551:role/GitHubActionsRole

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js 14
        uses: actions/setup-node@v2
        with:
          node-version: 14.x
          cache: 'npm'
      - run: npm ci
      - run: npm run lint
  lint-yaml:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Use Python 3.9
        uses: actions/setup-python@v2
        with:
          python-version: '3.9'
      - name: Lint YAML
        run: make lint-yamllint
  deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v2
      - uses: aws-actions/configure-aws-credentials@v1
        with:
          role-to-assume: ${{ env.AWS_PRODUCTION_ROLE_ARN }}
          aws-region: us-west-2
      - run: aws sts get-caller-identity
      - name: Use Node.js 14
        uses: actions/setup-node@v2
        with:
          node-version: 14.x
          cache: 'npm'
      - run: npm ci
      - name: Show CDK diff
        if: ${{ github.event_name == 'pull_request' }}
        run: npx cdk diff
      - name: Deploy CDK template
        if: ${{ github.event_name == 'push' }}
        run: npx cdk deploy --require-approval never
